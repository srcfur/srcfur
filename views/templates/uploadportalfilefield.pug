.fileFrame
    div
        img(id="filePreview")
        input(type="file" name="image")
    div
        .furaffinityFileField
            h2(class="labeled") Additional Tags (Furaffinity / E6)
                textarea(name="additional-tags")